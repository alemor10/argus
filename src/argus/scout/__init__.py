"""Scout — discovery. Finds candidates; the human decides.

Screener values are only ever a candidate filter: nothing from the screener
is persisted as an observation or reported as data — every number in a scout
digest comes from the v1 fetch→gate stack. See ARCHITECTURE.md, Scout.
"""
